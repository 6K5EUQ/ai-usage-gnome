# Build artifacts (regenerated by `make pack` / `make install`)
*.shell-extension.zip
schemas/gschemas.compiled

# Local credentials must never be committed — the extension reads these at
# runtime from your home directory, not from the repo.
.credentials.json
*credentials*.json

# Editor / OS noise
*.swp
*~
.DS_Store
.idea/
.vscode/
